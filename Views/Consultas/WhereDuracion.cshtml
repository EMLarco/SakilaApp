@model IEnumerable<SakilaApp.Models.Film>
@{
    ViewData["Title"] = "Películas con duración entre 90 y 120 minutos";
}

<div class="container mt-4">
    <h1 class="mb-4"><i class="bi bi-camera-reels"></i> @ViewData["Title"]</h1>

    <div class="table-responsive">
        <table class="table table-striped table-bordered table-hover">
            <thead class="table-primary">
                <tr>
                    <th>Título</th>
                    <th>Duración (min)</th>
                    <th>Descripción</th>
                    <th>Tarifa</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(m => item.Title)</td>
                        <td>@Html.DisplayFor(m => item.Length)</td>
                        <td>@(item.Description?.Length > 80 ? item.Description.Substring(0,80) + "..." : item.Description)</td>
                        <td>@Html.DisplayFor(m => item.RentalRate)</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <a asp-controller="Home" asp-action="Index" class="btn btn-secondary"><i class="bi bi-arrow-left"></i> Volver</a>
</div>
